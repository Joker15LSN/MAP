PRUNING_PROMPT_TEMPLATE = """你是一个数据库模式优化专家。
我们有一张包含许多列的宽表，我们需要将用户的问题转换为 SQL。

你的任务是检查用户的问题，并从提供的列表中选择一个**严格需要**的列的子集，这些列是回答问题所确定需要或可能需要的（包括 ID、必要的日期、度量值和条件）。
请勿更改列名。

[用户问题]
{question}

[宽表字段]
{fields}

将所选列的名称作为 JSON 数组输出，并放在 JSON 代码块中。
期望的输出格式：
```json
{{
    "status": "success",
    "selected_column_names": ["col1", "col2"]
}}
```
"""
