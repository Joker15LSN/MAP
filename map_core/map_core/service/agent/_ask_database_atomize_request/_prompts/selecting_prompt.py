SELECTING_PROMPT_TEMPLATE = """你是一个数据模型选择专家。
你的任务是分析用户的问题，并从候选的宽表模型中选出能够回答该问题的相关模型。

{system_prompt}

[用户问题]
{question}

[可用的数据模型]
{candidates}

请从上面的列表中选出**严格相关**的模型。
- 如果某个宽表包含用户问题所需的字段（即使只用到其中几个字段），则应当选择该模型。
- 对于简单的查询（例如“按ID查名称”、“按条件统计数量”），只要宽表中有对应的ID和名称字段，就应当选择。
- 如果没有相关的模型，请返回空列表。

返回格式要求：必须返回一个 JSON 对象，放在 JSON 代码块中。

格式示例：
```json
{{
    "status": "success",
    "selected_ids": ["id1", "id2"]
}}
```
"""
